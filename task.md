
У нас проблемы с чтением ABC файлов.
Видимо порт библиотеки был выполнен неточно, и где-то есть расхождения в обработке каких-то трансформов:
файлы в data/*.abc загружаются некорректно.
Для примера можно взять flo.abc: это должна быть компактная структура, а при загрузке файла во вьювер мы видим кучу запчастей разбросанных везде.
Проверь, что с трансформами, явно что-то не то - пивоты, матрицы, порядок вращений, что-то не так.
Методично проверяй и сравнивай вычисления во всех исходниках, файл за файлом, НЕ ПРОПУСКАЯ АБСОЛЮТНО НИЧЕГО, НИ ОДНОГО ФАЙЛА ИЛИ АЛГОРИТМА.
Нужно полное сравнение PARITY и проверка алгоритмов.
Можешь читать файлы и смотреть бинарные дампы, использовать alembic.exe для дампа данных, проверок и прочего.
Если что - версия alembic находится в _ref/, проверяй исходники там.
Можешь сбилдить её если надо для тестов.
Спрашивай вопросы если надо.


Подготочь исследование и отчёт - в PLAN2.md
